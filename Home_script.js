// Simulate getting the user's display name (could be from session or API)
const userDisplayName = "John Doe";  // Replace this with dynamic user data

// Set the welcome message dynamically
document.getElementById('welcomeMessage').innerHTML = `Welcome, ${userDisplayName}`;
